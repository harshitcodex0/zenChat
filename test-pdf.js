const pdfParse = require('pdf-parse'); const fs = require('fs'); async function run() { console.log('Creating dummy PDF'); const buf = Buffer.from('%PDF-1.4\n1 0 obj\n<<\n/Type /Catalog\n/Pages 2 0 R\n>>\nendobj\ntrailer\n<<\n/Root 1 0 R\n>>\n%%EOF'); try { console.log('Parsing PDF'); const data = await pdfParse(buf); console.log('Data:', data.text); } catch(e) { console.error('Error parsing:', e); } } run();

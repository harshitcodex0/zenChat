require("dotenv").config(); const { Pool } = require("pg"); const pool = new Pool({ connectionString: process.env.DATABASE_URL }); const collId = "cmth7brx200006c3kxphf4j2s"; pool.query("SELECT id, title, status FROM document WHERE collectionId = $1 LIMIT 10", [collId]).then(r => { console.log("Docs:", JSON.stringify(r.rows)); pool.end(); }).catch(e => { console.error("Err:", e.message); pool.end(); });

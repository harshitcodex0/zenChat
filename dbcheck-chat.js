require("dotenv").config(); const { Pool } = require("pg"); const pool = new Pool({ connectionString: process.env.DATABASE_URL }); pool.query("SELECT c.id, c.title, ck.\"B\" as collection_id FROM chat c LEFT JOIN \"_ChatKnowledge\" ck ON c.id = ck.\"A\" ORDER BY c.\"createdAt\" DESC LIMIT 5").then(r => { console.log("Recent chats with collections:", JSON.stringify(r.rows, null, 2)); pool.end(); }).catch(e => { console.error(e.message); pool.end(); });

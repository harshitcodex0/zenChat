import { PrismaClient } from './lib/generated/prisma/client'; import { Pool } from 'pg'; import { PrismaPg } from '@prisma/adapter-pg'; import 'dotenv/config'; const pool = new Pool({ connectionString: process.env.DATABASE_URL }); const prisma = new PrismaClient({ adapter: new PrismaPg(pool) }); async function main() { try { const user = await prisma.user.create({ data: { id: 'test-user-id', name: 'Test User', email: 'test@example.com', emailVerified: true } }); console.log('User created'); const account = await prisma.account.create({ data: { id: 'test-account-id', accountId: '123456', providerId: 'github', userId: 'test-user-id' } }); console.log('Account created'); } catch (e) { console.error('Error:', e); } finally { await prisma.user.deleteMany({ where: { id: 'test-user-id' } }).catch(()=>null); await pool.end(); } } main();

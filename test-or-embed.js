const { createOpenAI } = require('@ai-sdk/openai'); const { embed } = require('ai'); require('dotenv').config(); const or = createOpenAI({ baseURL: 'https://openrouter.ai/api/v1', apiKey: process.env.OPENROUTER_API_KEY }); embed({ model: or.embedding('jinaai/jina-embeddings-v2-base-en'), value: 'hello' }).then(res => console.log('Dimension:', res.embedding.length)).catch(console.error);

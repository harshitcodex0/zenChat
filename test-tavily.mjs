import * as dotenv from 'dotenv'; dotenv.config(); async function main() { const response = await fetch('https://api.tavily.com/search', { method: 'POST', headers: { 'Content-Type': 'application/json' }, body: JSON.stringify({ api_key: process.env.TAVILY_API_KEY, query: 'latest news', search_depth: 'basic', max_results: 3 }) }); console.log(response.status); console.log(await response.text()); } main();
